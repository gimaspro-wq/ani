# Frontend Environment Variables
# Copy this file to .env.local and customize as needed

# Optional: Override the app URL (defaults to window.location.origin in browser, http://localhost:3000 on server)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Backend API URL for authentication and user data (defaults to http://localhost:8000)
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

